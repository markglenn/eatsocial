var form = $("#register-buttons-<%= @event.id %> > form")

form.attr( "action", "<%= subscribe_event_path(@event) %>" )
form.find( "span.ui-btn-text" ).text("I'm going")

$.notifyBar({
    html: "I'm no longer going!",
    delay: 1000,
    animationSpeed: "fast"
  });
