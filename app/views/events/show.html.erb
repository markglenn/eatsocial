<p id="notice"><%= notice %></p>

<p>
  <b>Location:</b>
  <%= @event.location %>
</p>

<p>
  <b>Time:</b>
  <%= @event.time %>
</p>

<p>
  <b>Image location:</b>
  <%= @event.image_location %>
</p>

<% if @event.owner == current_person %>
  <%= button_to 'Delete this event?', @event, :method => :delete %>
<% elsif @event.people.exists?(current_person) %>
  <%= button_to 'Unregister', { :action => 'unsubscribe', :method => :post, :id => @event.id } %>
<% else %>
  <%= button_to 'Register', { :action => 'subscribe', :method => :post, :id => @event.id } %>
<% end %>

<br />

<% if @event.owner == current_person %>
<%= link_to 'Edit', edit_event_path(@event) %> |
<% end %>
<%= link_to 'Back', events_path %>
